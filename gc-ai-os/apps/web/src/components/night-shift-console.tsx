"use client";
import { useEffect, useMemo, useState } from "react";
const PROJECTS=["JARVIS","AI video editor","Commercial Radar","Mourad"];
const DEFAULT_PROMPT=`Operate as JARVIS autonomous night-shift pilot. Work continuously until the deadline or until every achievable objective is verified. CODED ≠ DONE; VERIFIED = DONE. Inspect real repository state before acting. Plan work, execute with available agents/tools, run tests and builds, inspect failures, repair them, retry within bounded limits, and never claim success without executable evidence. Prioritize JARVIS, the AI video editor, Commercial Radar and Mourad. Keep changes clean, reversible and documented. Never disable tests, invent evidence, silently swallow failures, or loop forever. Critical destructive, financial, credential, irreversible production or human-approval actions must be blocked and recorded for owner review. Non-critical failures should self-heal and continue with the next safe mission. Preserve durable checkpoints so a process restart resumes rather than duplicates work.`;
function defaultDeadline(){const now=new Date(),d=new Date(now);d.setDate(now.getDate()+(now.getHours()>=9?1:0));d.setHours(9,0,0,0);const l=new Date(d.getTime()-d.getTimezoneOffset()*60000);return l.toISOString().slice(0,16)}
type Snapshot={run:null|{id:string;status:string;deadline:string;lastError:string|null;objectiveId:string|null};objective:null|{status:string;title:string};missions:Array<{id:string;title:string;status:string;attempt:number;maxAttempts:number}>};
export function NightShiftConsole(){const[prompt,setPrompt]=useState(DEFAULT_PROMPT),[deadline,setDeadline]=useState(defaultDeadline),[projects,setProjects]=useState(PROJECTS),[snapshot,setSnapshot]=useState<Snapshot|null>(null),[starting,setStarting]=useState(false),[error,setError]=useState<string|null>(null);const refresh=async()=>{try{const r=await fetch('/api/night-shift',{cache:'no-store'});setSnapshot(await r.json())}catch(e){setError(e instanceof Error?e.message:'Impossible de lire l’état Night Shift.')}};useEffect(()=>{void refresh();const t=window.setInterval(()=>void refresh(),5000);return()=>window.clearInterval(t)},[]);const running=snapshot?.run?.status==='queued'||snapshot?.run?.status==='running';const completed=useMemo(()=>snapshot?.missions.filter(m=>m.status==='completed').length??0,[snapshot]);const toggle=(p:string)=>setProjects(c=>c.includes(p)?c.filter(x=>x!==p):[...c,p]);const start=async()=>{setStarting(true);setError(null);try{const r=await fetch('/api/night-shift',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({prompt,deadline:new Date(deadline).toISOString(),projects})});const d=await r.json();if(!r.ok)throw new Error(d.error??'Le démarrage a échoué.');setSnapshot(d)}catch(e){setError(e instanceof Error?e.message:'Le démarrage a échoué.')}finally{setStarting(false)}};return <main className="night-shift"><section className="night-hero"><div><div className="eyebrow">JARVIS / AUTONOMOUS NIGHT SHIFT</div><h1>Tu dors. JARVIS travaille.</h1><p>Mission durable, vérifiée, et reprise après redémarrage du runtime.</p></div><div className={`night-status ${running?'is-running':''}`}><span className="night-status-dot"/>{running?'EN EXÉCUTION':snapshot?.run?.status?.toUpperCase()??'PRÊT'}</div></section><section className="night-grid"><div className="night-card night-main-card"><label className="night-label" htmlFor="night-prompt">Mission maître</label><textarea id="night-prompt" value={prompt} onChange={e=>setPrompt(e.target.value)} disabled={running||starting}/><div className="night-row"><div><label className="night-label" htmlFor="night-deadline">Deadline</label><input id="night-deadline" type="datetime-local" value={deadline} onChange={e=>setDeadline(e.target.value)} disabled={running||starting}/></div><div className="night-projects"><span className="night-label">Projets</span><div className="night-project-list">{PROJECTS.map(p=><button key={p} type="button" className={`night-project ${projects.includes(p)?'selected':''}`} onClick={()=>toggle(p)} disabled={running||starting}>{projects.includes(p)?'✓':'＋'} {p}</button>)}</div></div></div><button className="night-start" type="button" onClick={start} disabled={running||starting||projects.length===0}>{starting?'INITIALISATION…':running?'NIGHT SHIFT EN COURS':'LANCER LE NIGHT SHIFT'}</button>{error&&<p className="night-error">{error}</p>}</div><aside className="night-card night-proof-card"><div className="night-label">État vérifiable</div><div className="night-metric"><strong>{completed}</strong><span>missions terminées</span></div><div className="night-divider"/><div className="night-meta"><span>Run</span><strong>{snapshot?.run?.id?.slice(0,8)??'—'}</strong></div><div className="night-meta"><span>Objectif</span><strong>{snapshot?.objective?.status??'—'}</strong></div><div className="night-meta"><span>Deadline</span><strong>{snapshot?.run?new Date(snapshot.run.deadline).toLocaleString():'—'}</strong></div>{snapshot?.run?.lastError&&<div className="night-error">{snapshot.run.lastError}</div>}</aside></section>{snapshot?.missions?.length?<section className="night-card"><div className="night-label">Missions persistées</div><div className="night-missions">{snapshot.missions.map(m=><div className="night-mission" key={m.id}><span className={`mission-dot mission-${m.status}`}/><span className="mission-title">{m.title}</span><span className="mission-status">{m.status}</span><span className="mission-attempt">{m.attempt}/{m.maxAttempts}</span></div>)}</div></section>:null}</main>}
