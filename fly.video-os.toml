app = "generation-capable-video-os-1306948595"
primary_region = "cdg"

[build]
  dockerfile = "Dockerfile.video-os"

[http_service]
  internal_port = 80
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0
  processes = ["app"]
